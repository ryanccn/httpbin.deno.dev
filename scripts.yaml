scripts:
  dev:
    desc: start up the dev server
    cmd: deno run -A --watch mod.ts
  lint:
    desc: lint
    cmd: deno lint mod.ts
  benchmark:
    desc: benchmark this service vs. pie.dev
    cmd: deno run -A _benchmark.ts
